# Frontend Testing Guide

This document describes the testing infrastructure setup for Tushar Super Mart.

## Overview

The frontend is configured to support **Vitest** as the test runner with **React Testing Library** for component testing. Tests can be written in TypeScript and run in a jsdom environment that simulates a browser.

## Test Dependencies

The following test dependencies are included in `package.json`:

